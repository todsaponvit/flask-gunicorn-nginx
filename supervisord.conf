# supervisord.conf
[supervisord]
nodaemon=true

[program:nginx]
command=nginx -g "daemon off;"
autostart=true
autorestart=true
priority=10

[program:uwsgi]
command=uwsgi --ini /etc/uwsgi/uwsgi.ini
autostart=true
autorestart=true
priority=20
