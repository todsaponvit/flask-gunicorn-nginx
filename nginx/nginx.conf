# nginx/nginx.conf
server {
    listen 80;
    server_name _;
    
    location /static {
        alias /app/static;
    }

    location / {
        include uwsgi_params;
        uwsgi_pass unix:///tmp/uwsgi.sock;
    }
}
